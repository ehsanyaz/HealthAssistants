<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.savar_computer.healthassistants">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Profile" />
        <activity android:name=".EditProfile" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".Setting" />
        <activity android:name=".StepMenu" />
        <activity
            android:name=".Menu"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            >
        </activity>
        <activity android:name=".AddFoodMenu" />
        <activity android:name=".AddFood" />
        <activity android:name=".Weight" />
        <activity android:name=".AddActivity" />
        <activity android:name=".WeightCharts" />
        <activity android:name=".Advise"></activity>
    </application>

</manifest>